CREATE TABLE logins (userID INT PRIMARY KEY, username varchar(64), password varchar(64));
CREATE TABLE requests (requestID INT PRIAMRY KEY, ip varchar(64), username varchar(64), time varchar(64), page varchar(64));
INSERT INTO logins (username, password) VALUES ('Admin', 'Admin123')